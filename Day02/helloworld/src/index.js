import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './components/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


// CAMEL CASE
// hello world one two-> helloWorldOneTwo

// LOWER CAMEL CASE
// hello world one two-> helloWorldOneTwo

// UPPER CAMEL CASE
// hello world one two-> HelloWorldOneTwo
