export const addSum = (a, b) => {
    return a + b;
    }

export const divNum = (a, b) => { 
    return a / b;
    }
export default divNum

export const mulNum = (a, b) => {
    return a * b;
    }

export const subNum = (a, b) => {
    return a - b;
    }